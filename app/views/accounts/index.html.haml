= Account.count

- @accounts.each do |a|
  = a.name
  %br
  = a.users.first.name
  = a.users.first.role
  = link_to 'Delete', a, :method => :delete, :data => { :confirm => 'Are you sure?' }

%hr/
= link_to "New Account", new_account_path

